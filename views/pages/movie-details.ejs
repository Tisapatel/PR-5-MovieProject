<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= movie.title %> - Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    /* Banner Background */
    .banner {
      position: relative;
      background: linear-gradient(to right, rgba(0, 0, 0, 0.85), rgba(0, 0, 0, 0.3)),
        url('<%= movie.bannerImage %>') center center / cover no-repeat;
      min-height: 500px;
      display: flex;
      align-items: center;
      margin-bottom: 0;   
    }

    /* Overlay Content */
    .banner-overlay {
      position: relative;
      z-index: 2;
      color: #fff;
      padding: 20px;
    }

    /* Poster */
    .poster-img {
      border-radius: 10px;
      width: 100%;
      max-width: 300px;
      height: auto;
      box-shadow: 0 0 20px rgba(0,0,0,0.7);
    }

    /* Rating */
    .rating {
      background: #2c2c2c;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      display: inline-flex;
      align-items: center;
      margin-right: 1rem;
      font-size: 0.9rem;
    }
    .rating i {
      color: #f1c40f;
      margin-right: 0.3rem;
    }

    /* Buttons */
    .btn-book {
      background-color: #ff3f6c;
      border: none;
    }
    .btn-book:hover {
      background-color: #e6325d;
    }

    /* Movie Info Tags */
    .movie-info span {
      display: inline-block;
      margin-right: 0.5rem;
      margin-bottom: 0.5rem;
      background: #2c2c2c;
      padding: 0.3rem 0.6rem;
      border-radius: 5px;
      font-size: 0.85rem;
    }

    /* Responsive Fixes */
    @media (max-width: 768px) {
      .banner {
        text-align: center;
        min-height: 400px;
        padding-top: 40px;
      }
      .poster-img {
        margin-bottom: 20px;
        max-width: 220px;
      }
      .banner-overlay {
        padding: 15px;
      }
    }
  </style>
</head>
<body>

<header>
  <%- include('../partials/header') %>
</header>

<div class="container-fluid gx-0">
  <% if (movie.bannerImage) { %>
    <div class="banner">
      <div class="container banner-overlay">
        <div class="row align-items-center">
          
          <!-- Poster -->
          <div class="col-md-3 text-center">
            <img src="<%= movie.posterImage %>" class="poster-img img-fluid" alt="Poster">
          </div>

          <!-- Movie Details -->
          <div class="col-md-9 mt-3 mt-md-0">
            <h1 class="fw-bold"><%= movie.title %></h1>

            <!-- <div class="d-flex align-items-center my-2 flex-wrap">
              <div class="rating">
                <i class="bi bi-star-fill"></i> <%= movie.rating %>/10
              </div>
              <small>(<%= movie.votes %> Votes)</small>
            </div> -->

            <div class="movie-info my-3">
              <span><%= movie.language %></span><br>
              <span><%= movie.genre %></span><br>
              <span><%= movie.category %></span>
            </div>

            <div class="mt-3">
              <a href="#" class="btn btn-book btn-lg me-3">Book Tickets</a>
              <a href="#" class="btn btn-outline-light">Share</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>

</body>
</html>
